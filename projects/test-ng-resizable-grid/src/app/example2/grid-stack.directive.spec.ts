import { GridStackDirective } from './grid-stack.directive';

describe('GridStackDirective', () => {
  it('should create an instance', () => {
    const directive = new GridStackDirective();
    expect(directive).toBeTruthy();
  });
});

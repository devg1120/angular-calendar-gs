import { GridStackItemDirective } from './grid-stack-item.directive';

describe('GridStackItemDirective', () => {
  it('should create an instance', () => {
    const directive = new GridStackItemDirective();
    expect(directive).toBeTruthy();
  });
});
